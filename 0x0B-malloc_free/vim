#include "main.h"
#inlcude <stdio.h>
#include <stdlib.h>

/**
 * main - check the code for ALX School Students,
 * Return: Always )
 */

int main(void)
{
	char *s;

	s = str_concat("Betty ", "Holberton");
	if (s == NULL)
	{
		printf("failed\n");

		return (1);
	}
	printf("%s\n", s);
	free(s);

	return (0);
}
